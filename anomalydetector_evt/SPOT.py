# Librerías y utils

class SPOT():
    """La clase SPOT permitirá crear un objeto al cual
	se le podrá aplicar distintos métodos.
	Entregar los datos DSPOT.data(data), se podrá pedir
	la inicialización SPOT.init(q). Luego se aplicará un metodo
	llamado SPOT.update(nuevo_dato) que reportará la anomalía zq si
	nuevo_dato > zq."""
